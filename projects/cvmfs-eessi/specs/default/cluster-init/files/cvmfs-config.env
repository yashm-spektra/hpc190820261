# CVMFS-EESSI Configuration
# This file contains customizable settings for the CVMFS-EESSI installation

# CVMFS Cache quota in MB (default: 10000 = 10GB)
# Adjust based on available disk space
CVMFS_CACHE_QUOTA=10000

# CVMFS Cache base directory
# Auto-detection order: /mnt/nvme/cvmfs -> /mnt/cvmfs -> /var/lib/cvmfs
# Set this variable to override auto-detection
#CVMFS_CACHE_BASE=/var/lib/cvmfs

# CVMFS HTTP Proxy setting
# Options: DIRECT (no proxy), or http://proxy.server:port
CVMFS_PROXY=DIRECT

# Additional CVMFS repositories to enable (space-separated)
# Default EESSI repository is always enabled
#ADDITIONAL_REPOS="cms.cern.ch atlas.cern.ch"

# Enable debug logging (true/false)
DEBUG_LOGGING=false

# Backup directory for existing configurations
BACKUP_DIR=/var/lib/cvmfs-eessi-backup